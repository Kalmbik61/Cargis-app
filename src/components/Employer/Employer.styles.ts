import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    gap: 5,
  },
  text: {
    fontWeight: "500",
    fontSize: 13,
    lineHeight: 24,
  },
});

export default styles;
